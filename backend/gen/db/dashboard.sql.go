// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: dashboard.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const getDashboardSummary = `-- name: GetDashboardSummary :one
SELECT
  (SELECT count(*)::bigint FROM customers c
   WHERE c.tenant_id = $1 AND c.deleted_at IS NULL AND c.active = true
  ) AS active_customers,
  (SELECT count(*)::bigint FROM customers c
   WHERE c.tenant_id = $1 AND c.deleted_at IS NULL
     AND c.created_at >= now() - interval '30 days'
  ) AS new_customers_30d,
  (SELECT count(*)::bigint FROM sales s
   JOIN customers c ON c.id = s.customer_id AND c.tenant_id = s.tenant_id AND c.deleted_at IS NULL
   WHERE s.tenant_id = $1 AND s.deleted_at IS NULL AND s.status = 'draft'
  ) AS draft_sales,
  (SELECT count(*)::bigint FROM products p
   WHERE p.tenant_id = $1 AND p.deleted_at IS NULL AND p.active = true
     AND p.stock <= $2
  ) AS low_stock_alerts,
  (SELECT count(*)::bigint FROM sales s
   WHERE s.tenant_id = $1 AND s.deleted_at IS NULL AND s.status = 'confirmed'
  ) AS confirmed_sales_count,
  (SELECT coalesce(sum(s.total), 0)::numeric FROM sales s
   WHERE s.tenant_id = $1 AND s.deleted_at IS NULL AND s.status = 'confirmed'
  ) AS confirmed_sales_total
`

type GetDashboardSummaryParams struct {
	TenantID  pgtype.UUID `json:"tenant_id"`
	Threshold int32       `json:"threshold"`
}

type GetDashboardSummaryRow struct {
	ActiveCustomers     int64          `json:"active_customers"`
	NewCustomers30d     int64          `json:"new_customers_30d"`
	DraftSales          int64          `json:"draft_sales"`
	LowStockAlerts      int64          `json:"low_stock_alerts"`
	ConfirmedSalesCount int64          `json:"confirmed_sales_count"`
	ConfirmedSalesTotal pgtype.Numeric `json:"confirmed_sales_total"`
}

// Aggregate read model: depends on customers, sales, and products tables.
func (q *Queries) GetDashboardSummary(ctx context.Context, arg GetDashboardSummaryParams) (GetDashboardSummaryRow, error) {
	row := q.db.QueryRow(ctx, getDashboardSummary, arg.TenantID, arg.Threshold)
	var i GetDashboardSummaryRow
	err := row.Scan(
		&i.ActiveCustomers,
		&i.NewCustomers30d,
		&i.DraftSales,
		&i.LowStockAlerts,
		&i.ConfirmedSalesCount,
		&i.ConfirmedSalesTotal,
	)
	return i, err
}
