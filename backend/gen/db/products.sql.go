// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: products.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const countProducts = `-- name: CountProducts :one
SELECT count(*)::bigint
FROM products
WHERE tenant_id = $1 AND deleted_at IS NULL
  AND ($2::text = '' OR name ILIKE '%' || $2 || '%' OR sku ILIKE '%' || $2 || '%')
  AND ($3::boolean IS NULL OR active = $3)
`

type CountProductsParams struct {
	TenantID pgtype.UUID `json:"tenant_id"`
	Search   string      `json:"search"`
	Active   pgtype.Bool `json:"active"`
}

func (q *Queries) CountProducts(ctx context.Context, arg CountProductsParams) (int64, error) {
	row := q.db.QueryRow(ctx, countProducts, arg.TenantID, arg.Search, arg.Active)
	var column_1 int64
	err := row.Scan(&column_1)
	return column_1, err
}

const createProduct = `-- name: CreateProduct :one
INSERT INTO products (tenant_id, name, sku, price, stock, unit, barcode, active)
VALUES ($1, $2, $3, $4, $5, $6, $7, $8)
RETURNING id, tenant_id, name, sku, price, stock, unit, barcode, active, deleted_at, created_at, updated_at
`

type CreateProductParams struct {
	TenantID pgtype.UUID    `json:"tenant_id"`
	Name     string         `json:"name"`
	Sku      string         `json:"sku"`
	Price    pgtype.Numeric `json:"price"`
	Stock    int32          `json:"stock"`
	Unit     string         `json:"unit"`
	Barcode  pgtype.Text    `json:"barcode"`
	Active   bool           `json:"active"`
}

func (q *Queries) CreateProduct(ctx context.Context, arg CreateProductParams) (Product, error) {
	row := q.db.QueryRow(ctx, createProduct,
		arg.TenantID,
		arg.Name,
		arg.Sku,
		arg.Price,
		arg.Stock,
		arg.Unit,
		arg.Barcode,
		arg.Active,
	)
	var i Product
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.Name,
		&i.Sku,
		&i.Price,
		&i.Stock,
		&i.Unit,
		&i.Barcode,
		&i.Active,
		&i.DeletedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getProduct = `-- name: GetProduct :one
SELECT id, tenant_id, name, sku, price, stock, unit, barcode, active, deleted_at, created_at, updated_at
FROM products
WHERE id = $1 AND tenant_id = $2 AND deleted_at IS NULL
`

type GetProductParams struct {
	ID       pgtype.UUID `json:"id"`
	TenantID pgtype.UUID `json:"tenant_id"`
}

func (q *Queries) GetProduct(ctx context.Context, arg GetProductParams) (Product, error) {
	row := q.db.QueryRow(ctx, getProduct, arg.ID, arg.TenantID)
	var i Product
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.Name,
		&i.Sku,
		&i.Price,
		&i.Stock,
		&i.Unit,
		&i.Barcode,
		&i.Active,
		&i.DeletedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listLowStockProducts = `-- name: ListLowStockProducts :many
SELECT id, tenant_id, name, sku, price, stock, unit, barcode, active, deleted_at, created_at, updated_at
FROM products
WHERE tenant_id = $1 AND deleted_at IS NULL AND active = true
  AND stock <= $2
ORDER BY stock ASC, name ASC
LIMIT $3
`

type ListLowStockProductsParams struct {
	TenantID   pgtype.UUID `json:"tenant_id"`
	Threshold  int32       `json:"threshold"`
	LimitCount int32       `json:"limit_count"`
}

func (q *Queries) ListLowStockProducts(ctx context.Context, arg ListLowStockProductsParams) ([]Product, error) {
	rows, err := q.db.Query(ctx, listLowStockProducts, arg.TenantID, arg.Threshold, arg.LimitCount)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Product
	for rows.Next() {
		var i Product
		if err := rows.Scan(
			&i.ID,
			&i.TenantID,
			&i.Name,
			&i.Sku,
			&i.Price,
			&i.Stock,
			&i.Unit,
			&i.Barcode,
			&i.Active,
			&i.DeletedAt,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listProducts = `-- name: ListProducts :many
SELECT id, tenant_id, name, sku, price, stock, unit, barcode, active, deleted_at, created_at, updated_at
FROM products
WHERE tenant_id = $1 AND deleted_at IS NULL
  AND ($2::text = '' OR name ILIKE '%' || $2 || '%' OR sku ILIKE '%' || $2 || '%')
  AND ($3::boolean IS NULL OR active = $3)
ORDER BY created_at DESC
LIMIT $5 OFFSET $4
`

type ListProductsParams struct {
	TenantID    pgtype.UUID `json:"tenant_id"`
	Search      string      `json:"search"`
	Active      pgtype.Bool `json:"active"`
	OffsetCount int32       `json:"offset_count"`
	LimitCount  int32       `json:"limit_count"`
}

func (q *Queries) ListProducts(ctx context.Context, arg ListProductsParams) ([]Product, error) {
	rows, err := q.db.Query(ctx, listProducts,
		arg.TenantID,
		arg.Search,
		arg.Active,
		arg.OffsetCount,
		arg.LimitCount,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Product
	for rows.Next() {
		var i Product
		if err := rows.Scan(
			&i.ID,
			&i.TenantID,
			&i.Name,
			&i.Sku,
			&i.Price,
			&i.Stock,
			&i.Unit,
			&i.Barcode,
			&i.Active,
			&i.DeletedAt,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const softDeleteProduct = `-- name: SoftDeleteProduct :one
UPDATE products
SET deleted_at = now(), updated_at = now()
WHERE id = $1 AND tenant_id = $2 AND deleted_at IS NULL
RETURNING id, tenant_id, name, sku, price, stock, unit, barcode, active, deleted_at, created_at, updated_at
`

type SoftDeleteProductParams struct {
	ID       pgtype.UUID `json:"id"`
	TenantID pgtype.UUID `json:"tenant_id"`
}

func (q *Queries) SoftDeleteProduct(ctx context.Context, arg SoftDeleteProductParams) (Product, error) {
	row := q.db.QueryRow(ctx, softDeleteProduct, arg.ID, arg.TenantID)
	var i Product
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.Name,
		&i.Sku,
		&i.Price,
		&i.Stock,
		&i.Unit,
		&i.Barcode,
		&i.Active,
		&i.DeletedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const updateProduct = `-- name: UpdateProduct :one
UPDATE products
SET name = $1,
    sku = $2,
    price = $3,
    stock = $4,
    unit = $5,
    barcode = $6,
    active = $7,
    updated_at = now()
WHERE id = $8 AND tenant_id = $9 AND deleted_at IS NULL
RETURNING id, tenant_id, name, sku, price, stock, unit, barcode, active, deleted_at, created_at, updated_at
`

type UpdateProductParams struct {
	Name     string         `json:"name"`
	Sku      string         `json:"sku"`
	Price    pgtype.Numeric `json:"price"`
	Stock    int32          `json:"stock"`
	Unit     string         `json:"unit"`
	Barcode  pgtype.Text    `json:"barcode"`
	Active   bool           `json:"active"`
	ID       pgtype.UUID    `json:"id"`
	TenantID pgtype.UUID    `json:"tenant_id"`
}

func (q *Queries) UpdateProduct(ctx context.Context, arg UpdateProductParams) (Product, error) {
	row := q.db.QueryRow(ctx, updateProduct,
		arg.Name,
		arg.Sku,
		arg.Price,
		arg.Stock,
		arg.Unit,
		arg.Barcode,
		arg.Active,
		arg.ID,
		arg.TenantID,
	)
	var i Product
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.Name,
		&i.Sku,
		&i.Price,
		&i.Stock,
		&i.Unit,
		&i.Barcode,
		&i.Active,
		&i.DeletedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
