// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: suppliers.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const countSuppliers = `-- name: CountSuppliers :one
SELECT count(*)::bigint
FROM suppliers
WHERE tenant_id = $1 AND deleted_at IS NULL
  AND ($2::text = '' OR name ILIKE '%' || $2 || '%' OR COALESCE(document, '') ILIKE '%' || $2 || '%')
  AND ($3::boolean IS NULL OR active = $3)
`

type CountSuppliersParams struct {
	TenantID pgtype.UUID `json:"tenant_id"`
	Search   string      `json:"search"`
	Active   pgtype.Bool `json:"active"`
}

func (q *Queries) CountSuppliers(ctx context.Context, arg CountSuppliersParams) (int64, error) {
	row := q.db.QueryRow(ctx, countSuppliers, arg.TenantID, arg.Search, arg.Active)
	var column_1 int64
	err := row.Scan(&column_1)
	return column_1, err
}

const createSupplier = `-- name: CreateSupplier :one
INSERT INTO suppliers (tenant_id, name, document, email, phone, active)
VALUES ($1, $2, $3, $4, $5, $6)
RETURNING id, tenant_id, name, document, email, phone, active, deleted_at, created_at, updated_at
`

type CreateSupplierParams struct {
	TenantID pgtype.UUID `json:"tenant_id"`
	Name     string      `json:"name"`
	Document pgtype.Text `json:"document"`
	Email    pgtype.Text `json:"email"`
	Phone    pgtype.Text `json:"phone"`
	Active   bool        `json:"active"`
}

func (q *Queries) CreateSupplier(ctx context.Context, arg CreateSupplierParams) (Supplier, error) {
	row := q.db.QueryRow(ctx, createSupplier,
		arg.TenantID,
		arg.Name,
		arg.Document,
		arg.Email,
		arg.Phone,
		arg.Active,
	)
	var i Supplier
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.Name,
		&i.Document,
		&i.Email,
		&i.Phone,
		&i.Active,
		&i.DeletedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getSupplier = `-- name: GetSupplier :one
SELECT id, tenant_id, name, document, email, phone, active, deleted_at, created_at, updated_at
FROM suppliers
WHERE id = $1 AND tenant_id = $2 AND deleted_at IS NULL
`

type GetSupplierParams struct {
	ID       pgtype.UUID `json:"id"`
	TenantID pgtype.UUID `json:"tenant_id"`
}

func (q *Queries) GetSupplier(ctx context.Context, arg GetSupplierParams) (Supplier, error) {
	row := q.db.QueryRow(ctx, getSupplier, arg.ID, arg.TenantID)
	var i Supplier
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.Name,
		&i.Document,
		&i.Email,
		&i.Phone,
		&i.Active,
		&i.DeletedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listSuppliers = `-- name: ListSuppliers :many
SELECT id, tenant_id, name, document, email, phone, active, deleted_at, created_at, updated_at
FROM suppliers
WHERE tenant_id = $1 AND deleted_at IS NULL
  AND ($2::text = '' OR name ILIKE '%' || $2 || '%' OR COALESCE(document, '') ILIKE '%' || $2 || '%')
  AND ($3::boolean IS NULL OR active = $3)
ORDER BY created_at DESC
LIMIT $5 OFFSET $4
`

type ListSuppliersParams struct {
	TenantID    pgtype.UUID `json:"tenant_id"`
	Search      string      `json:"search"`
	Active      pgtype.Bool `json:"active"`
	OffsetCount int32       `json:"offset_count"`
	LimitCount  int32       `json:"limit_count"`
}

func (q *Queries) ListSuppliers(ctx context.Context, arg ListSuppliersParams) ([]Supplier, error) {
	rows, err := q.db.Query(ctx, listSuppliers,
		arg.TenantID,
		arg.Search,
		arg.Active,
		arg.OffsetCount,
		arg.LimitCount,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Supplier
	for rows.Next() {
		var i Supplier
		if err := rows.Scan(
			&i.ID,
			&i.TenantID,
			&i.Name,
			&i.Document,
			&i.Email,
			&i.Phone,
			&i.Active,
			&i.DeletedAt,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const softDeleteSupplier = `-- name: SoftDeleteSupplier :one
UPDATE suppliers
SET deleted_at = now(), updated_at = now()
WHERE id = $1 AND tenant_id = $2 AND deleted_at IS NULL
RETURNING id, tenant_id, name, document, email, phone, active, deleted_at, created_at, updated_at
`

type SoftDeleteSupplierParams struct {
	ID       pgtype.UUID `json:"id"`
	TenantID pgtype.UUID `json:"tenant_id"`
}

func (q *Queries) SoftDeleteSupplier(ctx context.Context, arg SoftDeleteSupplierParams) (Supplier, error) {
	row := q.db.QueryRow(ctx, softDeleteSupplier, arg.ID, arg.TenantID)
	var i Supplier
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.Name,
		&i.Document,
		&i.Email,
		&i.Phone,
		&i.Active,
		&i.DeletedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const updateSupplier = `-- name: UpdateSupplier :one
UPDATE suppliers
SET name = $3, document = $4, email = $5, phone = $6, active = $7, updated_at = now()
WHERE id = $1 AND tenant_id = $2 AND deleted_at IS NULL
RETURNING id, tenant_id, name, document, email, phone, active, deleted_at, created_at, updated_at
`

type UpdateSupplierParams struct {
	ID       pgtype.UUID `json:"id"`
	TenantID pgtype.UUID `json:"tenant_id"`
	Name     string      `json:"name"`
	Document pgtype.Text `json:"document"`
	Email    pgtype.Text `json:"email"`
	Phone    pgtype.Text `json:"phone"`
	Active   bool        `json:"active"`
}

func (q *Queries) UpdateSupplier(ctx context.Context, arg UpdateSupplierParams) (Supplier, error) {
	row := q.db.QueryRow(ctx, updateSupplier,
		arg.ID,
		arg.TenantID,
		arg.Name,
		arg.Document,
		arg.Email,
		arg.Phone,
		arg.Active,
	)
	var i Supplier
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.Name,
		&i.Document,
		&i.Email,
		&i.Phone,
		&i.Active,
		&i.DeletedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
